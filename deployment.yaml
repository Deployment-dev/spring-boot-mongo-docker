apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: spring-boot
  name: spring-controller
  namespace: spring-boot
spec:
  replicas: 1
  selector:
    matchLabels:
      name: spring-boot
  template:
    metadata:
      labels:
        name: spring-boot
    spec:
      imagePullSecrets:
      - name: dockerreposecret
      containers:
      - image: pachaanilkumar/spring-boot-mongo-docker:9a2c2dc
        name: spring-boot
        ports:
        - name: spring-boot
          containerPort: 8085
---
# Node Port Service
apiVersion: v1
kind: Service
metadata:
  labels:
    name: spring-boot
  name: spring-boot
  namespace: spring-boot
spec:
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8085
  selector:
    name: spring-boot
